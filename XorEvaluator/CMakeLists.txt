PROJECT(xor_eval) 

ADD_DEFINITIONS(--std=c++14)

INCLUDE_DIRECTORIES(
    Inc
    ${NEATCore_headers}
)

FILE(GLOB SOURCES
    "Src/*.cpp"
)

FILE(GLOB HEADERS
    "Inc/*.hpp"
)

ADD_LIBRARY(${PROJECT_NAME} STATIC ${HEADERS} ${SOURCES})
